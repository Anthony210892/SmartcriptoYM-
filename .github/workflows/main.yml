name: Flutter CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v3

      - name: 🛠️ Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: 🔧 Set environment HOME path
        run: |
          echo "HOME=$GITHUB_WORKSPACE" >> $GITHUB_ENV
          echo "✅ HOME set to: $GITHUB_WORKSPACE"

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🧹 Clean and build APK
        run: |
          flutter clean
          flutter build apk --debug --target-platform android-arm64 --build-name=1.0.0 --build-number=1

      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: SmartCriptoYM-debug
          path: build/app/outputs/flutter-apk/app-debug.apk
