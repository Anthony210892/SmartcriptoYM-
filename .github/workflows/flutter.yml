name: Flutter CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout repository
        uses: actions/checkout@v4

      - name: 🛠️ Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'  # ✅ Usa Dart 3.5 compatible
          channel: 'stable'

      - name: 📦 Install dependencies
        run: flutter pub get

      - name: 🧹 Clean previous builds
        run: flutter clean

      - name: 🏗️ Build Flutter APK
        run: |
          flutter build apk --debug
          echo "🔍 Buscando APK generado..."
          find $GITHUB_WORKSPACE -type f -name "*.apk" || echo "❌ No se encontró ningún APK"

          - name: 📤 Upload APK artifact (auto detect)
        uses: actions/upload-artifact@v4
        with:
          name: SmartCriptoYM-debug
          path: '**/*.apk'
